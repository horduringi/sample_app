<%= form_for(@relapse) do |f| %>
  <% if @relapse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relapse.errors.count, "error") %> prohibited this relapse from being saved:</h2>

      <ul>
      <% @relapse.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
